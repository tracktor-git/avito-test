const API_URL = 'http://localhost:3000';

const routes = {
  advertisements: `${API_URL}/advertisements`,
  advertisement: `${API_URL}/advertisement`,
  orders: `${API_URL}/orders`,
};

export default routes;
